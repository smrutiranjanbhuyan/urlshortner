<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SHORTNER | URL Shortener</title>
  <!-- ✅ Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800">

  <!-- ✅ Header -->
  <header class="fixed inset-x-0 top-0 z-50 bg-white shadow-md backdrop-blur-lg">
    <div class="max-w-7xl mx-auto flex items-center justify-between px-6 py-4">
      <!-- Logo -->
      <a href="/" class="flex items-center space-x-2">
        <img src="https://upload.wikimedia.org/wikipedia/commons/f/fa/Apple_logo_black.svg" alt="Logo" class="h-7">
        <span class="text-lg font-bold">Shortner</span>
      </a>
      
      <!-- Navigation -->
      <nav class="hidden md:flex space-x-6 font-medium text-gray-700">
        <a href="#" class="hover:text-blue-600 transition">How it works</a>
        <a href="#" class="hover:text-blue-600 transition">Get in touch</a>
      </nav>
      
      <!-- Auth Buttons -->
      <div class="flex space-x-3">
        <a href="/" class="hidden sm:inline-flex px-4 py-2 rounded-lg border border-gray-300 bg-white hover:bg-gray-100 text-sm font-semibold">Home</a>
        <a href="/signup" class="hidden sm:inline-flex px-4 py-2 rounded-lg border border-gray-300 bg-white hover:bg-gray-100 text-sm font-semibold">Sign up</a>
        <a href="/login" class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-500 font-semibold text-sm">Login</a>
      </div>
    </div>
  </header>

  <!-- ✅ Hero Section -->
  <section class="max-w-2xl mx-auto mt-32 px-4 text-center">
    <h1 class="text-3xl md:text-4xl font-bold mb-6">Shorten Your URL Instantly</h1>
    <div class="flex flex-col md:flex-row items-center gap-3 bg-white border border-gray-200 rounded-xl p-4 shadow-lg">
      <input type="text" placeholder="Enter your link here" id="search-bar"
        class="flex-1 w-full border border-gray-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-blue-500 outline-none">
      <button id="shortUrl"
        class="w-full md:w-auto px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg shadow-md transition">
        Shorten URL
      </button>
    </div>
  </section>

  <!-- ✅ Short URL Display -->
  <section class="flex flex-col items-center mt-10">
    <div class="bg-white max-w-md w-full p-5 rounded-xl shadow-md flex items-center gap-3">
      <input type="text" id="copyMe"
        class="flex-1 border border-blue-500 rounded-lg px-4 py-2"
        placeholder="Your shortened URL">
      <button onclick="copyMeOnClipboard()"
        class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium">
        Copy
      </button>
    </div>
    <p id="copyMsg" class="text-green-600 mt-3 text-sm hidden">Copied to clipboard!</p>
  </section>

  <!-- ✅ Footer -->
  <footer class="bg-gray-900 text-gray-300 mt-16">
    <div class="max-w-7xl mx-auto px-6 py-10 grid grid-cols-1 md:grid-cols-3 gap-8">
      <div>
        <h3 class="text-xl font-bold mb-3 text-white">Shortner</h3>
        <p class="text-sm">Streamline your links with ease. Shorten URLs effortlessly for better sharing.</p>
      </div>
      <div>
        <h3 class="text-xl font-bold mb-3 text-white">Contact</h3>
        <p class="text-sm">Phone: +91-79787-66971</p>
        <p class="text-sm">Email: smruti2004@gmail.com</p>
        <p class="text-sm">Address: 312 Lovely Street, NY</p>
      </div>
      <div>
        <h3 class="text-xl font-bold mb-3 text-white">Follow Us</h3>
        <div class="flex space-x-4">
          <a href="#" class="hover:text-white">Twitter</a>
          <a href="#" class="hover:text-white">Instagram</a>
          <a href="#" class="hover:text-white">Facebook</a>
        </div>
      </div>
    </div>
    <div class="text-center border-t border-gray-700 py-4 text-sm">© 2024 Shortner Inc. All rights reserved.</div>
  </footer>

  <!-- ✅ JS -->
  <script>
    document.querySelector('#shortUrl').addEventListener('click', () => {
      window.location.href = '/login';
    });

    function copyMeOnClipboard() {
      const copyText = document.querySelector("#copyMe");
      copyText.select();
      document.execCommand("copy");
      document.getElementById("copyMsg").classList.remove("hidden");
      setTimeout(() => {
        document.getElementById("copyMsg").classList.add("hidden");
      }, 2000);
    }
  </script>
</body>
</html>
